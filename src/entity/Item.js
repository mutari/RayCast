export class Item {

    point;

    /**
     * @param {Point} point
     */
    constructor(point) {
        this.point = point;
    }

    /**
     * @param {CanvasRenderingContext2D} graphics
     */
    draw(graphics) {

    }

}